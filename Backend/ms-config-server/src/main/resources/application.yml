server:
  port: 7071
spring:
  application:
    name: ms-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jorgecahui/SisMunicipio.git # ruta del repositorio git
          searchPaths: config-data
          default-label: def
  security:
    user:
      name: root
      password: 123456